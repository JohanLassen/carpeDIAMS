% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectral_filtering.R
\name{clean_pseudo_spectra}
\alias{clean_pseudo_spectra}
\title{Clean pseudo-spectra for many features (main batch entry)}
\usage{
clean_pseudo_spectra(
  targets,
  file_directory,
  fd = NULL,
  con = NULL,
  ms1_ppm = 0,
  grouper = ppm_upgma(ppm = 30),
  buffer = 50L,
  fdr_count = 30L,
  cor_count = 100L,
  min_count = 10L,
  report = TRUE,
  report_path = "pseudo_specs_report.pdf",
  mgf_dir = NULL,
  fetch_chunk_size = 200L,
  progress = TRUE
)
}
\arguments{
\item{targets}{Character vector of feature ids.}

\item{file_directory}{Path to pool/pointer parquets.}

\item{fd}{Optional `featureDefinitions()`-shaped frame; supplies
`mzmed` and `rtmed` per target.}

\item{con}{Optional pre-opened connection (else opened + closed internally).}

\item{ms1_ppm, ms2_ppm}{Numeric ppm tolerances.}

\item{buffer, fdr_count, cor_count, min_count}{Tunables passed downstream.}

\item{report, report_path}{PDF diagnostic report on/off + path.}

\item{mgf_dir}{Optional directory for per-feature MGFs.}

\item{fetch_chunk_size}{Features per DuckDB query.}

\item{progress}{Print a per-feature progress counter.}
}
\value{
A `data.table` matching the legacy 02_filter_features schema.
}
\description{
Orchestrates: connect → fetch → per-feature [process_pseudo_spec()] →
[select_pseudo_spec()] → optional plot/MGF. Returns a `data.table` in
the schema downstream `03_make_mgf.R` expects.
}
